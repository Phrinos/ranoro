rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to the database for any user that is signed
    // in to the application.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
