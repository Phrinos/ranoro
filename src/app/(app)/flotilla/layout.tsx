import React from 'react';

// Layout simplificado para evitar errores de compilación mientras se elimina el módulo antiguo
export default function OldFlotillaLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
