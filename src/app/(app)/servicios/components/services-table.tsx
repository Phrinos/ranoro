
"use client";

// This component is no longer used as the card layout is now implemented directly in the page.
// This file can be removed in a future cleanup.
import React from "react";

export const ServicesTable = React.memo(() => {
  return null;
});

ServicesTable.displayName = 'ServicesTable';
