
"use client"

import Calendar from 'react-calendar';
import 'react-calendar/dist/Calendar.css';

// You can export the Calendar directly or wrap it if you need to add custom logic or styling.
// For now, we'll re-export it to match the old 'NewCalendar' name if other components rely on it.
export const NewCalendar = Calendar;
