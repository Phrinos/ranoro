rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Regla de Producción:
    // Solo permite la lectura y escritura de archivos a usuarios
    // que están autenticados en tu aplicación.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
